export const DEFAULT_SETTINGS = {
  targetRatio: 0.4, // 默认 40%
  roundType: 'ceil', // ceil, floor, round
};

export const STORAGE_KEYS = {
  SETTINGS: 'ODT_USER_SETTINGS',
  RECORDS: 'ODT_RECORDS',
};

export const RECORD_TYPES = {
  OFFICE: 'office', // 已到岗
  LEAVE: 'leave',   // 请假
};
